import PagePlaceholder from '../components/PagePlaceholder';

export default function PostsPage() {
  return (
    <PagePlaceholder
      title="Posts"
      description="Blog posts and the block editor. Built out in a later task."
    />
  );
}
