import PagePlaceholder from '../components/PagePlaceholder';

export default function VersionsPage() {
  return (
    <PagePlaceholder
      title="Versions"
      description="Published version history with restore. Built out in a later task."
    />
  );
}
