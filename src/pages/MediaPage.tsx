import PagePlaceholder from '../components/PagePlaceholder';

export default function MediaPage() {
  return (
    <PagePlaceholder
      title="Media"
      description="The media library — direct-to-S3 uploads, asset grid, and picker. Built out in a later task."
    />
  );
}
