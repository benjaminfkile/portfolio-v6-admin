import PagePlaceholder from '../components/PagePlaceholder';

export default function PreviewPage() {
  return (
    <PagePlaceholder
      title="Preview"
      description="Live preview of the draft page, rendered by the public site inside an iframe (§7). Built out in a later task."
    />
  );
}
